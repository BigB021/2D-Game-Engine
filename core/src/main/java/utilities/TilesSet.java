package utilities;

import java.util.Set;

public class TilesSet {

    /** All tile‐IDs that should be treated as “solid” / collidable */
    public static final Set<Integer> COLLIDABLE_TILES = Set.of(
        155,
        200,
        201,
        202
        // … add as many as you need
    );

}
